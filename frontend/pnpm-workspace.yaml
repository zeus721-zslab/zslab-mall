# store를 node_modules(익명 볼륨) 내부에 두어 빌드·런타임 store 파일시스템을 일치시킨다.
# (bind-mount 소스와 별도 device였던 교차-디바이스 store 불일치→purge 트랩 근본 제거)
storeDir: /app/node_modules/.pnpm-store
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  vue-demi: true
