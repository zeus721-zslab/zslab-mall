name: frontend-ci

on:
  pull_request:
    branches:
      - main
    paths:
      - 'frontend/**'
      - '.github/workflows/**'
  push:
    branches:
      - main
    paths:
      - 'frontend/**'
      - '.github/workflows/**'

jobs:
  frontend-check:
    runs-on: ubuntu-latest
    env:
      # pnpm-workspace.yaml storeDir(/app…·컨테이너 전용)를 CI 러너 쓰기 가능 경로로 오버라이드.
      # setup-node의 cache:pnpm(pnpm store path)과 install이 동일 경로를 참조하게 job 전역 주입.
      npm_config_store_dir: ${{ github.workspace }}/.pnpm-store
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # action-setup은 setup-node의 cache:pnpm보다 먼저 와야 한다(pnpm 실행파일 선존재 필요).
      # 버전은 frontend/package.json의 packageManager 핀에서 자동 인식(루트 package.json 없음 → 경로 명시).
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          package_json_file: frontend/package.json

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          # working-directory와 무관하게 repo 루트 상대경로.
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # typecheck 스크립트가 'nuxt prepare && vue-tsc -b'라 prepare(.nuxt 타입 생성) 선행 포함.
      - name: Typecheck
        run: pnpm typecheck

      # vitest run — e2e/**는 vitest.config.ts exclude로 미수집(playwright 전용).
      - name: Unit tests (vitest)
        run: pnpm test
