spring:
  jpa:
    properties:
      hibernate:
        show_sql: false

management:
  endpoint:
    health:
      show-details: when-authorized

logging:
  level:
    root: WARN
    com.zslab.mall: INFO
